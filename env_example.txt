# ToolRosella environment example

# GitHub search. Optional, but recommended to avoid low unauthenticated limits.
GITHUB_TOKEN=your_github_token_here

# Repository analysis. Optional.
JINA_API_KEY=your_jina_api_key_here
DISABLE_DEEPWIKI=false

# Model provider selection: openai, deepseek, qwen, claude, bedrock, ollama.
MODEL_PROVIDER=openai

# OpenAI-compatible planner and generator configuration.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-5

# DeepSeek.
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Qwen.
QWEN_API_KEY=your_qwen_api_key_here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-3

# Claude.
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_BASE_URL=https://api.anthropic.com
CLAUDE_MODEL=claude-4-sonnet

# AWS Bedrock.
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
BEDROCK_MODEL=anthropic.claude-4-sonnet

# Ollama.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# General model parameters.
MODEL_TEMPERATURE=0.1
MODEL_MAX_TOKENS=8192
MODEL_TIMEOUT=300
MODEL_MAX_RETRIES=10

# CodeCheck can validate generated imports deterministically.
# Set true to let the node ask the configured LLM to repair invalid wrappers.
TOOLROSELLA_CODECHECK_REPAIR=false

# PlanningAgent writes a Claude-Code-like MCP run package by default.
# Set true to execute TOOLROSELLA_AGENT_COMMAND with the generated mcp.json.
TOOLROSELLA_RUN_PLANNING_AGENT=false
TOOLROSELLA_AGENT_COMMAND=claude

# Runtime root fallback used by the internal Code2MCP workflow.
TOOLROSELLA_ROOT=.
