简单可直接测试的查询
1. 测试数据验证工具 ✅ 最简单
检查这个空列表是否为空：[]
检查这个包含3个用户的列表是否为空：['user1', 'user2', 'user3']
2. 测试简单的比率计算 ✅ 简单，有直观结果
计算这个用户活跃度数据的 Palma 比率来衡量不平等程度：
[1000, 500, 200, 100, 50, 30, 20, 10, 5, 1]

这个数据表示10个用户的发帖数，第一个用户非常活跃，最后一个用户不活跃。
Palma 比率会告诉我们社区中活跃度的不平等程度。
比较两个社区的用户活跃度不平等：

社区A：[100, 100, 100, 100, 100]（很均匀）
社区B：[1000, 1, 1, 1, 1]（高度不平等）

分别计算它们的 Palma 比率，看看社区A的数值会更小。
3. 测试图的创建和分析 ✅ 中等复杂度，但完全自包含
创建一个简单的社交网络图：

用户互动数据：
- user1 转发了 user2 的推文
- user2 转发了 user3 的推文
- user3 转发了 user1 的推文
- user1 也转发了 user3 的推文

从这个边列表创建图，然后计算 Wiener 指数（衡量图的紧凑程度）。
示例数据：
source: [1, 2, 3, 1]
target: [2, 3, 1, 3]
4. 测试原始推文比率 ✅ 简单且直观
分析一个推文级联的原始推文比率：

级联中的事件序列：
- 时间1：user1 发布原始推文（内容ID: post1）
- 时间2：user2 转发了 post1
- 时间3：user3 转发了 post1
- 时间4：user2 再次转发了 post1

这个级联中有1个原始推文，3次转发。
原始推文比率应该是 1/4 = 25%
5. 完整的小型级联分析 ✅ 最实用的测试
分析一个完整的 Twitter 信息级联：

级联数据：
- 时间1: @Alice 发布推文"COVID疫苗安全吗？" (推文ID: tweet_123)
- 时间2: @Bob 转发了Alice的推文
- 时间3: @Carol 转发了Alice的推文  
- 时间4: @David 转发了Bob的转发
- 时间5: @Eve 转发了Carol的转发

请：
1. 检查这个级联是否为空
2. 计算原始推文比率（1个原创推文 vs 4个转发）
3. 计算活跃度的 Palma 比率（用户发帖数：Alice=1, Bob=1, Carol=1, David=1, Eve=1）
4. 创建推文转发网络图并计算其紧凑性
6. 测试社区检测 ✅ 展示复杂功能但简洁数据
分析一个小型社区的讨论网络：

用户互动数据（谁回复了谁）：
- user_A ↔ user_B
- user_B ↔ user_C
- user_C ↔ user_A
- user_D ↔ user_E
- user_E ↔ user_F

这个网络中有两个社区：
社区1: {A, B, C}（高度连接）
社区2: {D, E, F}（相对独立）

请用 communityCentricMeasurements 分析这个网络的社区结构。
7. 测试不同平台的网络 ✅ 展示平台特定工具
GitHub 协作网络分析：

开发者和项目的关系：
- 开发者1 贡献了 ProjectA, ProjectB
- 开发者2 贡献了 ProjectB, ProjectC
- 开发者3 贡献了 ProjectA, ProjectC
- 开发者4 贡献了 ProjectC

用 githubnetworkmeasurements 分析：
1. 哪个项目最受欢迎（被最多开发者贡献）
2. 开发者之间的协作紧密度
3. 项目之间的关系
8. 错误处理测试 ✅ 测试鲁棒性
测试工具的错误处理能力：

1. 传入空数据给 check_empty：[]
   预期：返回 True

2. 传入 None 给 palma_ratio：None
   预期：返回错误信息而不是崩溃

3. 创建空图：edges = []
   预期：能正确处理而不崩溃
9. 快速健康检查 ✅ 最快验证所有工具是否工作
快速测试所有主要功能：

1. 数据验证：check_empty([])
   预期：True

2. 比率计算：palma_ratio([100, 50, 25, 10, 5])
   预期：返回一个数字（衡量不平等性）

3. 图论：创建一个3节点的简单三角形图
   预期：Wiener指数 = 3

4. 级联分析：计算 [1, 0, 0, 0] 的原始推文比率（4条数据中1个原创）
   预期：0.25 或 25%

5. 类初始化：创建 cascade 对象
   预期：成功创建对象

全部通过 = 所有工具都正常工作！
推荐测试顺序
先测试验证工具（查询1-2）— 最快看到结果
再测试比率计算（查询3-4）— 能直观看到数值
然后测试图论（查询5-6）— 验证复杂功能
最后测试类初始化（查询7-8）— 确保所有工具可用
